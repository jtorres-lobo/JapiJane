<script>
    document.addEventListener("DOMContentLoaded", async () => {
    const input = document.querySelector('input[name="order"]');
    const button = document.querySelector("button.query");
    const order_information = document.querySelector("#order_information");

    const sendOrder = async (value) => {
        try {
            if (value.length === 0) throw new Error("");
            const res = await fetch(`https://api.andesoms.com/public/treasury/gift_cards/${value}/balance?token={{ shop.metafields.andes_oms.public_token }}`, { method: "GET" });
            const resJson = await res.json();

            if (res.status === 200) {
                const { balance, expiration_date } = resJson;
                //const { order, label } = getStatus(status);
                order_information.innerHTML = "";
                order_information.innerHTML = `
                    <div class="order_title">
                        <h3>Gift-Card: ${value}</h3>
						<p>
							Saldo disponible: ${balance}<br/><br/>
							Fecha Vencimiento: ${expiration_date}<br /><br/>
  						</p>
                    </div>
                `;
            } else {
                order_information.innerHTML = `
                    <div class="order_title">
                        <h3>La gift-card no existe</h3>
                    </div>
                `;
            }
        } catch (error) {
            order_information.innerHTML = `
                <div class="order_title">
                    <h3>La gift-card no existe</h3>
                </div>
            `;
        }
    };

    button.addEventListener("click", async () => {
        order_information.innerHTML = `
            <div class="order_title">
                <h3>Cargando...</h3>
            </div>
        `;
        await sendOrder(input.value);
    });
  
    function triggerEvent(element, event) {
		const clickEvent = new Event(event);
		element.dispatchEvent(clickEvent);
    }
  
    function getParameterByName(name, url) {
    	var match = RegExp('[?&]' + name + '=([^&]*)').exec(url);
    	return match && decodeURIComponent(match[1].replace(/\+/g, ' '));
	};
    
//     const trackingId = getParameterByName('order_name', window.location.search);
//     if (trackingId != null) {
// 		console.log(trackingId);
// 		document.getElementById('order').value = trackingId;
//       	triggerEvent(document.getElementById('order-submit'), 'click');
//     }
});
</script>
<style>
        #check_order,
        *
        *:after
        *:before {
            margin: 0;
            padding: 0;
            outline: none !important;
        }
        #check_order {
            
            letter-spacing: 0.8px;
            width: 100%;
            max-width: 700px;
            margin: 0 auto;
          	margin-bottom:200px;
        }

        #order_form {
            display: flex;
            flex-direction: row;
        }

        .form_title {
            text-align: center;
            margin-bottom: 1rem;
          	font-size:2rem;
        }
        .order_input {
            width: 100%;
            height: 46px;
            border: 1px solid black;
            margin-right: 0.5rem;
            padding: 0rem 1rem;
          	font: message-box;
        }

        .query {
            background-color: #3e3d54;
            border: 0;
            padding: 0rem 1rem;
            cursor: pointer;
            display: flex;
            border: 1px solid black;
              align-items: center;
        }
        .query svg {
            width: 24px;
            height: 24px;
            fill: white;
        }

        #order_information {
            margin: 0 auto;
            width: 100%;
            max-width: 700px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .order_title {
            text-align: center;
            margin: 1.5rem 0rem;
            font-size: 1.1rem;
            margin: 1.5rem 0rem;
            line-height: 12px;
        }
        .order_title h3 {
          font-weight:bold !important;
          font-size:1.5rem;
        }
  
        .order_status {
            font-size: 1.2rem;
            margin-bottom: 2rem;
        }
        .node {
            height: 24px;
            width: 24px;
            border-radius: 50%;
            display: inline-block;
            transition: all 1000ms ease;
        }

        .activated {
            box-shadow: 0px 0px 3px 2px rgba(81, 61, 114, 0.1);
        }
        .activated + p {
            font-weight: bold;
            color: black !important;
        }

        .divider {
            height: 40px;
            width: 6px;
            margin-left: 9px;
            transition: all 800ms ease;
        }

        #order_information li p {
            display: inline-block;
            margin-left: 25px;
            font-style: italic;
          	font-size:1.1rem;
        }

        #order_information li {
            list-style: none;
            display: flex;
            flex-direction: row;
            align-items: center;
            line-height: 1px;
        }

        .active_color {
            background-color: #3e3d54;
            font-weight: bold;
        }
        .active_color + p {
            color: #3e3d54;
        }

        .grey {
            background-color: rgba(201, 201, 201, 1);
        }
        .grey + p {
            color: rgba(0, 0, 0, 0.5);
        }
    </style>
<div class="page-width">
  <br />
  <br />
  <div class="grid">
	<section id="check_order">
            <h1 class="form_title">Consulta Gift-Card</h1>
      		<br />
            <div id="order_form">
                <input
                    type="text"
                    id="order"
                    name="order"
                    class="order_input"
                    placeholder="Escribe el número de tu gift-card..."
                />
              
                <button id="order-submit" class="query">
                    <svg viewBox="0 0 24 24">
                        <path
                            d="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"
                        />
                        <path d="M0 0h24v24H0z" fill="none" />
                    </svg>
                </button>
            </div>
      			
            <div id="order_information"></div>
        </section>
  </div>
</div>
